[project]
name = "biblioteca-ucr-grecia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
    "xmltodict>=1.0.4",
]

[project.scripts]
biblioteca = "main:main"
